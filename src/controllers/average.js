const axios = require('axios');
const Car = require('../models/car');

module.exports = {
    async store(req, res) {
        const { } = req.body;

        const car = await Dev.create({
            
        });

        return res.json(dev);
    },
    average(req, res){
        this.store(req, res);

    }
}